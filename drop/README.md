# Drop

Upload your Letterboxd export zip here (letterboxd.com → Settings → Import & Export → Export your data) — from any device via GitHub's "Add file → Upload files". A workflow imports it into `data/`, deletes the zip, refreshes enrichment/themes/suggestions/stats, and republishes the site.
